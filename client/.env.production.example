# Example production environment variables for Viral Views frontend
# Copy this file to `.env.production` in the client directory and fill in your real values

# No custom frontend environment variables required by default
# If you add any, use the VITE_ prefix, e.g.:
# VITE_API_URL=https://api.yourdomain.com
